# 依赖
node_modules/

# Next.js 构建产物
.next/
out/

# 环境变量与密钥（绝不提交）
.env
.env.local
.env.*.local

# 运行时配置数据库（含 ADMIN_SECRET_KEY / 各 API 密钥明文，绝不提交）
data/settings.json

# GSC 服务账号凭证（含 GCP 权限，挂进容器/服务器用，绝不提交）
gsc-service-account.json
*.service-account.json
gcp-*.json

# 运行时用户数据（含 PII：买家邮箱）
data/leads.json

# 日志与系统文件
*.log
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts
